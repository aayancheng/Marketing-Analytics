# =============================================================================
# MarketingAnalytics — Consolidated Python Requirements (all 5 models)
# Single .venv at /Users/aayan/MarketingAnalytics/.venv
# =============================================================================

# --- Shared baseline (all models) ---
pandas==2.2.0
numpy==1.26.4
scikit-learn==1.4.0
shap==0.45.0
pyarrow>=15.0.0
matplotlib>=3.8.0
jupyter>=1.0.0
ipykernel>=6.29.0
fastapi==0.110.0
uvicorn[standard]==0.27.0
joblib==1.3.2
scipy==1.17.0

# --- m01 + m02: UCI Online Retail dataset ---
ucimlrepo>=0.0.7
openpyxl==3.1.2
lightgbm==4.3.0

# --- m02: CLV probabilistic baseline ---
lifetimes>=0.11.3

# --- m03: Churn propensity ---
imbalanced-learn>=0.12.0
# lightgbm already above

# --- m04: Marketing Mix Model (Bayesian) ---
pymc-marketing>=0.11.0
arviz>=0.19.0
# numpyro + jax optional (faster MCMC) — install separately if needed:
# pip install numpyro jax jaxlib

# --- m05: Next Best Offer (collaborative filtering) ---
implicit>=0.7.2
